inherits: base.yaml

experiment:
  name: "self_rag_dense_full"

architecture:
  name: "self_rag"

self_rag:
  num_candidates: 3

retrieval:
  method: "dense"
  embedding_model: "text-embedding-3-small"

evaluation:
  max_concurrency: 2

data:
  subset_size: null  # Full validation set
