inherits: base.yaml

experiment:
  name: "vanilla_hybrid_full"

retrieval:
  method: "hybrid"
  bm25_weight: 0.5
  dense_weight: 0.5
  embedding_model: "text-embedding-3-small"

data:
  subset_size: null  # Full validation set
