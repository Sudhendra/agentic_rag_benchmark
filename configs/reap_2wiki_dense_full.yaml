inherits: base.yaml

experiment:
  name: "reap_2wiki_dense_full"

architecture:
  name: "reap_rag"

reap:
  max_iterations: 5
  max_active_requirements: 2
  max_context_tokens: 3000
  max_docs: 8

retrieval:
  method: "dense"
  top_k: 3

evaluation:
  max_concurrency: 2

data:
  dataset: "2wikimultihop"
  subset_size: null
