inherits: base.yaml

experiment:
  name: "ircot_hybrid_full"

architecture:
  name: "ircot_rag"

ircot:
  max_steps: 4
  max_context_tokens: 3000
  max_docs: 8

retrieval:
  method: "hybrid"
  top_k: 3
  bm25_weight: 0.5
  dense_weight: 0.5
  embedding_model: "text-embedding-3-small"

data:
  subset_size: null  # Full validation set
