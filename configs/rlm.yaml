inherits: base.yaml

experiment:
  name: "rlm"

architecture:
  name: "recursive_lm"

rlm:
  max_depth: 3
  memoization: true
  max_context_tokens: 4000

retrieval:
  method: "bm25"
  top_k: 5

data:
  subset_size: 100
