inherits: base.yaml

experiment:
  name: "rlm_dense"

architecture:
  name: "recursive_lm"

rlm:
  max_depth: 3
  memoization: true
  max_context_tokens: 4000

retrieval:
  method: "dense"
  top_k: 5
  embedding_model: "text-embedding-3-small"

data:
  subset_size: 100
