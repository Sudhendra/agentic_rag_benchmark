inherits: base.yaml

experiment:
  name: "reap_musique_dense_full"

architecture:
  name: "reap_rag"

reap:
  max_iterations: 5
  max_active_requirements: 2
  max_context_tokens: 3000
  max_docs: 8

retrieval:
  method: "dense"
  top_k: 3
  embedding_model: "text-embedding-3-small"

evaluation:
  max_concurrency: 2

data:
  dataset: "musique"
  subset_size: null
