inherits: base.yaml

experiment:
  name: "react_hybrid_full"

architecture:
  name: "react_rag"

react:
  max_iterations: 7

retrieval:
  method: "hybrid"
  bm25_weight: 0.5
  dense_weight: 0.5
  embedding_model: "text-embedding-3-small"

evaluation:
  max_concurrency: 3  # Lower than default (5) since ReAct makes multiple LLM calls per question

data:
  subset_size: null  # Full validation set
